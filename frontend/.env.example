# Frontend environment variables
# Copy this to .env.local and fill in values for your deployment.
# In development, the Vite proxy handles /api routing — no URL needed.

# Backend URL for deployed builds (Render URL after deployment)
# VITE_API_URL=https://your-backend.onrender.com
